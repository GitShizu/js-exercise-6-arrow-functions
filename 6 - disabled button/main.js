// Crea un pulsante in HTML. Usa JavaScript per impostare un attributo 'disabled' sul pulsante usando setAttribute().

window.addEventListener('load', ()=>{
    const button = document.getElementById('btn');
    button.setAttribute('disabled', '');
})

